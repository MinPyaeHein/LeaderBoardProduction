default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: postgres
  password: root
  host: localhost

development:
  <<: *default
  database: leader_board_new_db

test:
  <<: *default
  database: leader_board_new_db
# production: <%= ENV['DATABASE_URL'] %>
